/**
 * @interface Renderer
 */

/**
 * @summary creates a new element
 * @function
 * @name Renderer#create(options)
 * @param {Object} options key/value object to set values on new element
 * @returns {HtmlElement} the new element
 */

/**
 * @summary updates an existing element
 * @function
 * @name Renderer#update(element, options)
 * @param {HtmlElement} element the element to update
 * @param {Object} options key/value object to set values on new element
 * @return {HtmlElement} the updated element
 */

import * as bootstrap from './bootstrap'

/**
 * Create an object that groups the functions with the file being the key
 * of the renderer. Therefore, when a form is selected a configuration object
 * can set so the right key can be selected in this object to find the correct
 * function
 */
export {
  bootstrap
}
